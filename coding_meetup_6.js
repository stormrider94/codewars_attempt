function isSameLanguage(list) {
    // thank you for checking out the Coding Meetup kata :)
    return list.every(dev=>dev.language==list[0].language)
  }
  //functions tht return booleans are the best